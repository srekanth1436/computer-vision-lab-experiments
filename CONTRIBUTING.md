# Contributing

1. Create a new branch.
2. Keep each experiment inside its own numbered folder.
3. Use relative paths instead of computer-specific paths.
4. Save generated files inside the experiment's `output` folder.
5. Run `python check_repository.py` before committing.
6. Open a pull request with a clear description.
